/** @type {import('next').NextConfig} */
const nextConfig = {
  // Node.js server mode for aaPanel / VPS hosting (output: 'export' removed)
  images: {
    unoptimized: false,
  },
};
export default nextConfig;
